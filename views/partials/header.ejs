<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp V11</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
    <nav class="navbar navbar-default navcolor">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">YelpCamp V11</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <!--pseudo code: if no user
                    Then show login and register-->
                    
                    
                    <% if(!currentUser){ %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/register">Sign Up</a></li>
                    <!-- else, show logout link -->
                    <% } else { %>
                    <li><a href="#">Signed In As <%=currentUser.username.toUpperCase()%></a></li>
                    <li><a href="/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <!--just having error exist isnt enough. So we need to check error.length > 0
    if empty array of error as it is an array is not undefinied. if there is anything in this array.
    display the error.-->
    
    <% if(error && error.length > 0) {%>
    <div class="container">
        <div class="alert alert-danger" role="alert">
           <h4><%=error %></h4> 
        </div>
    </div>
    <% } %>
    
    <% if(success && success.length > 0) { %>
    <div class="container">
        <div class="alert alert-success" role="alert">
           <h4><%=success %></h4> 
        </div>
    </div>
    <% } %>
    
    